
# 📚 تصفية المحتوى في لينكس باستخدام أدوات الطرفية

**التاريخ:** الخميس، 6 نوفمبر 2025  
**المستوى:** متوسط  
**الحالة:** 🟢 مكتمل

---

## 🎯 الأهداف (Goals / Objectives)

- تعلم كيفية عرض الملفات الكبيرة بطريقة تفاعلية.  
- اكتساب مهارات استخراج وفرز وتصفية أجزاء من الملفات النصية.  
- استخدام أدوات الطرفية لتعديل النصوص وتحليل النتائج بدون محرر.

---

## 📝 الملخص والمفاهيم الأساسية (Summary & Core Concepts)

هذا الدرس يركز على أدوات الطرفية التي تسهّل التعامل مع الملفات النصية الكبيرة، وتساعد في تصفية المحتوى وتحليله بسرعة.  
بدلاً من فتح الملفات في محرر، يمكن استخدام أوامر مثل `grep`, `cut`, `sort`, `wc` وغيرها لاستخراج معلومات محددة، فرزها، وعدّها.

### 📑 مفاهيم رئيسية
- **more / less:** عرض تفاعلي للملفات صفحة صفحة.  
- **head / tail:** عرض أول أو آخر أسطر من الملف.  
- **grep / cut / sort / wc:** أدوات تصفية وتحليل المحتوى النصي.

---

## ⚙️ الأوامر والتطبيق العملي (Practical Commands & Examples)

### 1. عرض الملفات الكبيرة

#### `more`

```bash
cat /etc/passwd | more
```

- يعرض الملف صفحة صفحة.  
- يسمح بالتمرير للأسفل فقط.  
- للخروج: اضغط `q`.

#### `less`

```bash
less /etc/passwd
```

- عرض تفاعلي مع إمكانية التمرير للأعلى والأسفل.  
- يدعم البحث داخل الملف.  
- لا يترك أثر في الطرفية بعد الخروج.

---

### 2. عرض أجزاء محددة من الملف

#### `head`

```bash
head /etc/passwd
head -n 5 /etc/passwd
```

- يعرض أول 10 أسطر افتراضيًا.  
- يمكن تحديد عدد الأسطر باستخدام `-n`.

#### `tail`

```bash
tail /etc/passwd
tail -n 5 /etc/passwd
```

- يعرض آخر 10 أسطر افتراضيًا.  
- يمكن تحديد عدد الأسطر باستخدام `-n`.

---

### 3. فرز الأسطر

#### `sort`

```bash
cat /etc/passwd | sort
```

- يرتب الأسطر أبجديًا.  
- مفيد للملفات غير المرتبة.

---

### 4. البحث والتصفية

#### `grep`

```bash
cat /etc/passwd | grep "/bin/bash"
grep -v "false\|nologin" /etc/passwd
```

- يبحث عن الأسطر التي تحتوي على `/bin/bash`.  
- `-v` يستبعد الأسطر التي تحتوي على كلمات معينة.

---

### 5. استخراج حقول محددة

#### `cut`

```bash
cut -d ":" -f1 /etc/passwd
```

- `-d ":"` يحدد الفاصل.  
- `-f1` يعرض الحقل الأول (اسم المستخدم).

---

### 6. استبدال الرموز

#### `tr`

```bash
cat /etc/passwd | tr ':' ' '
```

- يستبدل كل `:` بمسافة.

---

### 7. عرض النتائج في جدول

#### `column`

```bash
cat /etc/passwd | tr ':' ' ' | column -t
```

- يعرض النتائج في جدول مرتب.  
- يستخدم بعد `tr` لتحسين التنسيق.

---

### 8. عدّ الأسطر

#### `wc`

```bash
cat /etc/passwd | wc -l
```

- يحسب عدد الأسطر.  
- مفيد لمعرفة عدد النتائج بعد التصفية.

---

## 📊 النتائج والدروس المستفادة (Outcomes & Key Takeaways)

- **النتيجة الإيجابية:** تمكنت من تصفية وتحليل ملفات النظام باستخدام أدوات الطرفية.  
- **التحدي الذي واجهني:** فهم الفروقات بين `grep` و `cut` ودمجهم مع أدوات أخرى.  
- **درس أساسي:** أدوات الطرفية توفر تحكمًا سريعًا وفعالًا في تحليل الملفات بدون الحاجة لمحررات رسومية.

---

## 🔗 الكلمات المفتاحية والمصادر (Keywords & Resources)

### 🏷️ الكلمات المفتاحية (Keywords / Tags)
`Linux`, `Terminal`, `Filtering`, `Grep`, `Cut`, `Sort`, `WC`, `Column`, `TR`, `Head`, `Tail`

### 📚 مصادر للتعمق (Further Reading)
1. [Linux Command Line Tools](https://linuxize.com/post/linux-commands-for-filtering-text/)  
2. [Grep and Cut Tutorial](https://www.geeksforgeeks.org/grep-command-in-unixlinux/)  
3. [Advanced Text Processing](https://tldp.org/LDP/abs/html/textproc.html)
```
