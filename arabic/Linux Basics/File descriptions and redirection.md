
# 📚 أوصاف الملفات وإعادة التوجيه في لينكس

**التاريخ:** الثلاثاء، 4 نوفمبر 2025  
**المستوى:** متوسط  
**الحالة:** 🟢 مكتمل

---

## 🎯 الأهداف (Goals / Objectives)

- فهم مفهوم موصف الملف (File Descriptor) في نظام لينكس.  
- تعلم كيفية استخدام إعادة التوجيه والأنابيب للتحكم في الإدخال والإخراج.

---

## 📝 الملخص والمفاهيم الأساسية (Summary & Core Concepts)

في لينكس، كل ملف أو عملية إدخال/إخراج يتم تمثيلها برقم يسمى **موصف الملف (FD)**.  
النواة (Kernel) تستخدم هذا الرقم لتتبع الملفات المفتوحة لكل عملية.  
يشبه الأمر تذكرة دخول:  
- التذكرة = رقم FD  
- الشخص = الملف  
- الموظف = نظام التشغيل  

### 📑 مفاهيم رئيسية
- **File Descriptor (FD):** رقم مميز لكل ملف أو عملية إدخال/إخراج.  
- **STDIN / STDOUT / STDERR:** أوصاف الإدخال والإخراج القياسية في لينكس.

---

## ⚙️ الأوامر والتطبيق العملي (Practical Commands & Examples)

### 1. الأوصاف الثلاثة الأساسية في لينكس

| الوظيفة                                | الرقم | الإسم    |
| -------------------------------------- | ----- | -------- |
| الإدخال القياسي (مثل الكيبورد)         | 0     | `STDIN`  |
| الإخراج القياسي (النتائج)              | 1     | `STDOUT` |
| إخراج الأخطاء (رسائل تشفير أو تحذير)   | 2     | `STDERR` |

---

### 2. أمثلة على استخدام FD

#### مثال باستخدام `cat`

```bash
cat
````

- ينتظر إدخال من المستخدم عبر FD 0
- يعرض الإدخال مباشرة عبر FD 1
- مثال: كتبت "ssd" → يعرض "ssd"

---

#### مثال باستخدام `find`

```bash
find /etc/ -name shadow
```

- إذا تم العثور على الملف → يظهر عبر FD 1
- إذا تم رفض الوصول لبعض المجلدات → يظهر عبر FD 2

---

### 3. إعادة التوجيه

#### إعادة توجيه الأخطاء إلى `/dev/null`

```bash
find /etc/ -name shadow 2>/dev/null
```

- `2>` تعني إعادة توجيه FD 2
- `/dev/null` جهاز وهمي يتجاهل البيانات

---

#### إعادة توجيه النتائج إلى ملف

```bash
find /etc/ -name shadow 2>/dev/null > result.txt
```

- `>` تعني إعادة توجيه FD 1 إلى ملف

---

#### إعادة توجيه `STDOUT` و `STDERR` إلى ملفات منفصلة

```bash
find /etc/ -name shadow 2> stdt.txt 1> smd.txt
```

- الأخطاء → `stdt.txt`
- النتائج → `smd.txt`

---

#### إعادة توجيه `STDIN` من ملف

```bash
cat < result.txt
```

- الإدخال يأتي من الملف بدل الكيبورد

---

#### إعادة توجيه `STDOUT` مع الإلحاق

```bash
find /etc/ -name passwd >> result.txt 2>/dev/null
```

- `>>` تعني إضافة النتائج دون حذف المحتوى السابق

---

#### إدخال متعدد الأسطر باستخدام `EOF`

```bash
cat << EOF > result.txt
```

- يفتح إدخال يدوي حتى كتابة `EOF`
- `EOF` هنا تعني "نهاية الإدخال"، ليست جزءًا من الملف

---

### 4. استخدام الأنابيب `|`

- تربط أمرين: إخراج الأول → إدخال الثاني

```bash
ls /etc | grep passwd
```

- `ls` يعرض الملفات
- `grep` يفلتر النتائج التي تحتوي على "passwd"

---

#### ملاحظات مهمة

- الأنابيب تنقل `STDOUT` فقط
- لنقل الأخطاء أيضًا:

```bash
command 2>&1 | command
```

---

## 📊 النتائج والدروس المستفادة (Outcomes & Key Takeaways)

- **النتيجة الإيجابية:** فهمت كيفية التحكم في تدفق البيانات باستخدام FD وإعادة التوجيه.
- **التحدي الذي واجهني:** التمييز بين `STDOUT` و `STDERR` في الأوامر المعقدة.
- **درس أساسي:** إعادة التوجيه والأنابيب أدوات قوية لتنظيم الإخراج وتحليل النتائج.

---

## 🔗 الكلمات المفتاحية والمصادر (Keywords & Resources)

### 🏷️ الكلمات المفتاحية (Keywords / Tags)

`Linux`, `File_Descriptor`, `STDIN`, `STDOUT`, `STDERR`, `Redirection`, `Pipes`, `Shell`

### 📚 مصادر للتعمق (Further Reading)

1. [Linux File Descriptors Explained](https://linuxize.com/post/file-descriptors-in-linux/)
2. [Redirection in Bash](https://tldp.org/LDP/abs/html/io-redirection.html)
3. [Advanced Bash Scripting Guide](https://tldp.org/LDP/abs/html/)
